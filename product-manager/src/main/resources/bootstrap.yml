spring:
  application:
    name: product-manager
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
